ATM Cash Withdrawal
If you request cash from an ATM, it processes the transaction once and does not dispense money multiple times for a single request.

Code Example (C - ATM Withdrawal Process)ATM Cash Withdrawal
If you request cash from an ATM, it processes the transaction once and does not dispense money multiple times for a single request.

Code Example (C - ATM Withdrawal Process)
#include <stdio.h>

int main() {
    int withdrawal_done = 0; // Flag to track withdrawal
    int amount;

    printf("Enter withdrawal amount: ");
    scanf("%d", &amount);

    if (withdrawal_done == 0) {
        printf("Dispensing cash: $%d\n", amount);
        withdrawal_done = 1; // Mark transaction as done
    } else {
        printf("Transaction already completed.\n");
    }

    return 0;
}

